name: hapbilgiler
description: "Hap Bilgiler - KPSS sınav hazırlık uygulaması."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  # Essential packages for the game
  flag: ^7.0.2
  flutter_screenutil: ^5.9.3
  gauge_indicator: ^0.4.3
  
  # Animation packages
  animations: ^2.1.0
  lottie: ^3.1.0
  spring: ^2.0.2
  confetti: ^0.7.0
  
  # HTTP and state management
  http: ^1.5.0
  provider: ^6.1.1
  
  # Additional utilities
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  vibration: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/animations/
    - assets/audio/
    - assets/lottie/
